
# Input: current location as list [x,y] and direction moving in
# Output: new location
def move(loc, dir) :
    # when east, x+1
    # when north, y+1
    # when west, x-1
    # when south, y-1
    return loc